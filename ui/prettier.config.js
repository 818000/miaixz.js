/**
 * Defines the standalone source formatter configuration for @miaixz/ui.
 *
 * @public
 */
const configuration = {
  printWidth: 100,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default configuration;
