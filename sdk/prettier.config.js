/**
 * Defines the standalone source formatting contract for `@miaixz/sdk`.
 *
 * @public
 */
const configuration = {
  printWidth: 100,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default configuration;
